# To excecute

add your secrets on `TokenRefreshWorker.kt`

```kotlin
            val response = authService.getAccessToken(
                clientId = "",
                clientSecret = ""
            )
```